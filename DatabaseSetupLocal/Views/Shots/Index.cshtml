@using DatabaseSetupLocal.Data
@model List<UserShots>

@{
    ViewBag.Title = "User List";
    Layout = "_Layout";
}

<div>

    <table>
        <tr>
            <th>
                @Html.DisplayNameFor(Model => Model.First().UserName)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>

                <td>
                    @Html.ActionLink("Years", "Years", new {userId = item.Id}) |
                </td>
            </tr>
        }
    </table>
    
@Html.ActionLink("Add new user", "AddUser", null, null, new {@class = "btn btn-primary", @style = "margin-top: 20px"}) <br/>
    

</div>